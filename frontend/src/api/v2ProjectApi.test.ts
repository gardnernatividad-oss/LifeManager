import { beforeEach, describe, expect, it, vi } from "vitest";
import { apiClient } from "./client";
import { createV2Project, deactivateV2Project, getV2Project, listV2Projects, reactivateV2Project, updateV2Project } from "./v2ProjectApi";
vi.mock("./client", () => ({ apiClient: { get: vi.fn(), post: vi.fn(), patch: vi.fn() } }));
describe("v2ProjectApi", () => { beforeEach(() => vi.clearAllMocks()); it("uses Workspace-scoped endpoints", async () => { vi.mocked(apiClient.get).mockResolvedValue({ data: {} }); await listV2Projects("workspace-a", { page: 1, page_size: 25 }); await getV2Project("workspace-a", "project-1"); expect(apiClient.get).toHaveBeenNthCalledWith(1, expect.stringContaining("/api/v2/workspaces/workspace-a/projects"), { params: { page: 1, page_size: 25 } }); expect(apiClient.get).toHaveBeenNthCalledWith(2, expect.stringContaining("/projects/project-1")); }); it("preserves write payloads", async () => { vi.mocked(apiClient.post).mockResolvedValue({ data: {} }); vi.mocked(apiClient.patch).mockResolvedValue({ data: {} }); const create = { category_id: "category-1", leader_user_id: "user-1", name: "Mudanza" }; await createV2Project("workspace-a", create); await updateV2Project("workspace-a", "project-1", { name: "Casa", lock_version: 2 }); await deactivateV2Project("workspace-a", "project-1", 3); await reactivateV2Project("workspace-a", "project-1", 4); expect(apiClient.post).toHaveBeenNthCalledWith(1, expect.stringContaining("/projects"), create); expect(apiClient.patch).toHaveBeenCalledWith(expect.stringContaining("/projects/project-1"), { name: "Casa", lock_version: 2 }); expect(apiClient.post).toHaveBeenNthCalledWith(2, expect.stringContaining("/deactivate"), { lock_version: 3 }); expect(apiClient.post).toHaveBeenNthCalledWith(3, expect.stringContaining("/reactivate"), { lock_version: 4 }); }); });
