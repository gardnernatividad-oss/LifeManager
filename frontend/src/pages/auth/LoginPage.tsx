import { PagePlaceholder } from "../../components/common/PagePlaceholder";

export function LoginPage() {
  return <PagePlaceholder title="Login" description="Authentication will be added in a future stage." />;
}
